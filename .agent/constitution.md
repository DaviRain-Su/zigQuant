# ZigQuant Agent Constitution

> 本项目的核心开发原则与 AI 代理行为准则

---

## 🎯 项目使命

构建一个**高性能、低延迟、内存安全**的量化交易框架，结合 Hummingbot 的做市/套利能力和 Freqtrade 的策略回测/自动交易能力。

---

## 📐 核心原则

### 1. 文档优先 (Documentation-First)

- **所有功能必须先有文档，后有代码**
- Story 定义需求 → Docs 定义设计 → Code 实现功能
- 文档是唯一的真相来源 (Single Source of Truth)

### 2. 增量迭代 (Incremental Development)

- 从 MVP 开始，逐步添加功能
- 每个 Story 必须可独立测试和验证
- 优先交付核心价值，延后优化

### 3. 代码质量

#### 内存安全
```zig
// ✅ 好的做法
pub fn processOrder(allocator: std.mem.Allocator, order: Order) !void {
    defer allocator.free(order.id);
    // 确保资源释放
}

// ❌ 避免
pub fn processOrder(order: Order) void {
    // 内存泄漏风险
}
```

#### 错误处理
```zig
// ✅ 显式错误处理
const result = try exchange.submitOrder(request);

// ❌ 忽略错误
const result = exchange.submitOrder(request) catch unreachable;
```

#### 性能意识
```zig
// ✅ 使用 Arena Allocator 处理临时数据
var arena = std.heap.ArenaAllocator.init(allocator);
defer arena.deinit();
const temp_data = try arena.allocator().alloc(u8, size);

// ❌ 频繁分配释放
for (items) |item| {
    const data = try allocator.alloc(u8, item.size);
    defer allocator.free(data);
}
```

### 4. 安全第一 (Security-First)

- **永远不要硬编码 API 密钥**
- 所有敏感数据必须加密存储
- 所有交易操作必须审计日志
- 生产环境必须启用 ReleaseSafe 模式

### 5. 测试驱动 (Test-Driven)

- 核心模块测试覆盖率 > 90%
- 每个 Story 完成时必须有对应测试
- 使用 Mock 进行集成测试
- 性能基准测试定期执行

---

## 🤖 AI 代理行为准则

### 代码生成

1. **理解需求**
   - 仔细阅读 Story 和相关 Docs
   - 如有疑问，先询问澄清，不要猜测

2. **设计优先**
   - 大型功能先写设计文档
   - 讨论架构决策
   - 考虑边界条件

3. **实现规范**
   - 遵循 Zig 官方风格指南
   - 使用有意义的命名
   - 添加必要的注释（但代码应自解释）
   - 每个公共函数必须有文档注释

4. **增量提交**
   - 小步快跑，频繁验证
   - 每次只完成一个明确的任务
   - 确保每次提交都可编译

### 文档维护

1. **同步更新**
   - 代码变更时，同步更新文档
   - Bug 修复记录在对应的 bugs.md
   - 新增功能在 features/ 下创建文档

2. **清晰表达**
   - 使用简洁的语言
   - 提供代码示例
   - 绘制必要的图表

3. **可追溯性**
   - 每个 feature 文档链接到对应的 story
   - 文件路径使用相对路径
   - 保持文档结构与代码结构一致

### 问题解决

1. **Bug 修复流程**
   ```
   发现 Bug → 在 bugs.md 记录
       ↓
   分析根因 → 更新 bugs.md
       ↓
   实现修复 → 添加测试
       ↓
   验证修复 → 标记已解决
   ```

2. **性能问题**
   - 先测量，后优化
   - 使用 Profiler 定位瓶颈
   - 记录优化前后的对比数据

3. **设计变更**
   - 重大变更需要 ADR (Architecture Decision Record)
   - 讨论权衡和备选方案
   - 评估影响范围

---

## 🔄 标准开发流程 (Mandatory Development Workflow)

> **重要**: 每次功能开发都必须严格遵循此流程，不得跳过任何步骤。

### 开发流程

```
1️⃣ 编码实现 (Coding)
    ↓
2️⃣ 单元测试 (Unit Tests)
    ↓
3️⃣ 集成测试 (Integration Tests)
    ↓
4️⃣ 文档更新 (Documentation Update)
    ↓
5️⃣ 继续下一个功能 (Next Feature)
```

### 详细说明

#### 1️⃣ 编码实现 (Coding)

- 实现 Story 或功能需求中定义的功能
- 遵循代码质量标准和命名规范
- 确保代码可以编译通过
- 处理所有错误情况
- 添加必要的代码注释

**完成标准**:
- ✅ 代码编译无错误、无警告
- ✅ 所有公共函数有文档注释
- ✅ 错误处理完整
- ✅ 内存安全（无泄漏、无悬空指针）

---

#### 2️⃣ 单元测试 (Unit Tests)

**必须完成**:
- 为所有新增的公共函数编写单元测试
- 测试覆盖率 > 85%
- 测试所有边界条件和错误路径
- 使用有意义的测试名称

**测试内容**:
```zig
test "function name - normal case" {
    // 测试正常情况
}

test "function name - edge case: empty input" {
    // 测试边界条件
}

test "function name - error case: invalid input" {
    // 测试错误处理
}
```

**运行测试**:
```bash
# 运行模块测试
zig test src/path/to/module.zig

# 运行所有测试
zig build test --summary all
```

**完成标准**:
- ✅ 所有单元测试通过
- ✅ 测试覆盖率 > 85%
- ✅ 测试包含正常、边界、错误三类情况

---

#### 3️⃣ 集成测试 (Integration Tests)

**必须完成**:
- 测试模块间交互
- 对于网络相关功能，使用 testnet 环境测试
- 测试完整的业务流程

**集成测试示例**:
```zig
// tests/integration/exchange_test.zig
test "Exchange Connector - getTicker from testnet" {
    var connector = try HyperliquidConnector.create(allocator, config, logger);
    defer connector.deinit();

    const ticker = try connector.getTicker(.{ .base = "ETH", .quote = "USDC" });
    try std.testing.expect(ticker.bid.gt(Decimal.ZERO));
}
```

**运行集成测试**:
```bash
# 运行集成测试
zig build test-integration

# 或特定测试
zig build test-ws
```

**完成标准**:
- ✅ 所有集成测试通过
- ✅ Testnet 连接成功（如适用）
- ✅ 完整业务流程验证通过

---

#### 4️⃣ 文档更新 (Documentation Update)

**必须更新**:
1. **Feature 文档** (`docs/features/<module>/README.md`)
   - 更新功能描述
   - 添加新的 API 说明
   - 更新代码示例
   - 更新版本号和最后更新时间

2. **API 文档** (`docs/features/<module>/api.md`)
   - 添加新函数的完整 API 文档
   - 包含参数说明、返回值、错误类型

3. **实现文档** (`docs/features/<module>/implementation.md`)
   - 记录实现细节和设计决策

4. **测试文档** (`docs/features/<module>/testing.md`)
   - 更新测试覆盖率
   - 记录测试用例

5. **变更日志** (`docs/features/<module>/changelog.md`)
   - 添加版本变更记录

6. **项目进度** (`docs/PROGRESS.md`)
   - 更新 Story 完成状态
   - 更新模块完成度

**文档模板**:
```markdown
## v0.x.x - YYYY-MM-DD

### Added
- 新增 `functionName()` 方法
- 支持 XXX 功能

### Changed
- 优化 YYY 性能

### Fixed
- 修复 ZZZ bug
```

**完成标准**:
- ✅ 所有相关文档已更新
- ✅ 代码示例可运行
- ✅ 版本号和日期正确
- ✅ PROGRESS.md 反映最新进度

---

#### 5️⃣ 继续下一个功能 (Next Feature)

只有在完成上述所有步骤后，才能开始下一个功能的开发。

**检查清单**:
- [ ] 代码实现完整
- [ ] 单元测试通过（覆盖率 > 85%）
- [ ] 集成测试通过
- [ ] 文档已更新
- [ ] 无待办事项 (TODO)
- [ ] 代码已审查

**如果有任何一项未完成，不得继续下一步！**

---

### 流程执行示例

**场景**: 实现 `HyperliquidConnector.createOrder()` 方法

```bash
# 1️⃣ 编码实现
# 编辑 src/exchange/hyperliquid/connector.zig
# 实现 createOrder 方法和 Ed25519 签名集成

zig build  # 确保编译通过

# 2️⃣ 单元测试
# 在 src/exchange/hyperliquid/connector.zig 中添加测试
test "createOrder - normal limit order" { ... }
test "createOrder - edge case: zero price" { ... }
test "createOrder - error: invalid symbol" { ... }

zig test src/exchange/hyperliquid/connector.zig

# 3️⃣ 集成测试
# 编辑 tests/integration/hyperliquid_test.zig
test "createOrder - testnet integration" { ... }

zig build test-integration

# 4️⃣ 文档更新
# 更新以下文件:
- docs/features/hyperliquid-connector/README.md
- docs/features/hyperliquid-connector/api.md
- docs/features/hyperliquid-connector/changelog.md
- docs/PROGRESS.md

# 5️⃣ 验证完成
# 检查所有步骤都已完成，然后开始下一个方法 cancelOrder()
```

---

### AI 代理执行约束

**作为 AI 代理，我必须**:

1. ✅ **每次实现功能后，自动进入测试阶段**
   - 不等待用户提醒
   - 主动编写和运行测试

2. ✅ **测试通过后，自动更新文档**
   - 不跳过文档更新步骤
   - 确保文档与代码同步

3. ✅ **明确告知用户当前进度**
   - 清楚地说明当前在哪个阶段
   - 测试结果和文档更新状态

4. ✅ **不得跳过任何步骤**
   - 即使用户催促，也要完成所有步骤
   - 如果时间紧迫，缩小功能范围，而非跳过流程

5. ✅ **记录所有待办事项**
   - 如果某个步骤暂时无法完成，记录在 TODO 中
   - 在继续下一步前解决所有 TODO

---

### 违规处理

**如果发现违反流程的情况**:

1. 立即停止当前工作
2. 回退到上一个完成的阶段
3. 补充缺失的测试或文档
4. 重新验证所有步骤

**示例**:
```
❌ 错误: 实现了 3 个方法，但没有写测试就继续下一个功能

✅ 正确:
1. 实现 createOrder()
2. 测试 createOrder()
3. 文档更新
4. 实现 cancelOrder()
5. 测试 cancelOrder()
6. 文档更新
```

---

## 🚫 禁止事项

### 代码层面

- ❌ 使用 `@panic` 或 `unreachable`（除非逻辑上不可能）
- ❌ 忽略错误返回值
- ❌ 硬编码魔法数字
- ❌ 过度抽象（YAGNI 原则）
- ❌ 未测试的代码合并
- ❌ 提交注释掉的代码

### 安全层面

- ❌ 硬编码密钥/密码
- ❌ 以明文记录敏感信息
- ❌ 禁用安全检查
- ❌ 使用不安全的随机数生成器
- ❌ 跳过输入验证

### 流程层面

- ❌ 没有 Story 就开始编码
- ❌ 功能完成不更新文档
- ❌ 跳过代码审查
- ❌ 直接修改生产配置

---

## ✅ 推荐实践

### 命名规范

```zig
// 类型: PascalCase
pub const OrderManager = struct { ... };

// 函数: camelCase
pub fn submitOrder(...) !Order { ... }

// 常量: SCREAMING_SNAKE_CASE
pub const MAX_ORDERS: u32 = 1000;

// 变量: snake_case
const order_id = "abc123";
```

### 文件组织

```
src/
├── core/           # 核心类型和工具
├── exchange/       # 交易所连接器
├── strategy/       # 策略框架
├── backtest/       # 回测引擎
└── utils/          # 通用工具
```

### 错误处理层次

```zig
// 1. 可恢复错误 - 使用 error union
pub fn submitOrder(...) !Order { ... }

// 2. 编程错误 - 使用 assert
std.debug.assert(amount.isPositive());

// 3. 不可能发生 - 使用 unreachable (谨慎)
switch (status) {
    .open, .filled => {},
    else => unreachable,  // 已在上层过滤
}
```

---

## 📊 质量门禁

每个 Story 完成前必须检查：

- [ ] 功能实现完整
- [ ] 单元测试通过 (覆盖率 > 85%)
- [ ] 集成测试通过
- [ ] 文档已更新
- [ ] 代码已审查
- [ ] 性能达标（如适用）
- [ ] 安全检查通过
- [ ] 无编译警告

---

## 🔄 持续改进

### 每周回顾

- 检查 roadmap 进度
- 更新技术债务清单
- 评估性能指标
- 收集用户反馈

### 每月反思

- 回顾架构决策
- 评估技术栈选择
- 优化开发流程
- 更新 Constitution

---

## 📝 模板

### Story 模板
参见: `stories/templates/story-template.md`

### Feature 文档模板
参见: `docs/features/README.md`

### ADR 模板
参见: `docs/decisions/template.md`

---

## 🎓 学习资源

- [Zig 语言参考](https://ziglang.org/documentation/master/)
- [Zig 标准库文档](https://ziglang.org/documentation/master/std/)
- [Freqtrade 文档](https://www.freqtrade.io/)
- [Hummingbot 文档](https://docs.hummingbot.org/)

---

*Last updated: 2025-01-24*
*Version: 1.1* - 新增标准开发流程 (Mandatory Development Workflow)
