# libxev 集成方案 - zigQuant 性能优化

**库地址**: https://github.com/mitchellh/libxev  
**作者**: Mitchell Hashimoto (HashiCorp 创始人)  
**状态**: Production-ready (已用于 Ghostty 终端)  
**评估时间**: 2025-12-25

---

## 🎯 为什么选择 libxev？

### ✅ 核心优势

| 特性 | 说明 | 对 zigQuant 的价值 |
|------|------|------------------|
| **Proactor 模式** | 工作完成通知（vs 就绪通知） | ✅ 更适合交易系统（订单完成通知） |
| **io_uring 支持** | Linux 最快 I/O（超越 epoll） | ✅ 极致性能（HFT 级别） |
| **零运行时分配** | 无 GC，性能可预测 | ✅ 低延迟交易（无卡顿） |
| **Zig 原生** | 完美集成，tree-shaking | ✅ 二进制体积小，编译时优化 |
| **跨平台** | Linux/macOS/WASI | ✅ 开发和生产环境统一 |
| **内置线程池** | 可选后台任务 | ✅ 混合使用（I/O + 计算） |
| **生产就绪** | Ghostty 正在使用 | ✅ 稳定性有保证 |

### 🆚 vs 其他方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **libxev** | ✅ Zig 原生<br>✅ io_uring<br>✅ 零分配 | ⚠️ 需要学习 Proactor 模式 | **实时交易** ✅ |
| **手写 Actor** | ✅ 完全控制 | ❌ 开发成本高<br>❌ 需处理跨平台 | 如果有 3 周时间 |
| **std.Thread** | ✅ 简单 | ❌ 无异步 I/O<br>❌ 性能较低 | **参数优化器** ✅ |
| **async/await** | ✅ Zig 支持 | ⚠️ 语法复杂<br>⚠️ 生态不成熟 | 未来考虑 |

**结论**: 
- 实时交易模块 → **libxev** ✅
- 参数优化器 → **std.Thread 线程池** ✅
- 回测引擎 → **单线程**（无需改动）✅

---

## 📐 架构设计

### 整体架构：混合模型

```
┌─────────────────────────────────────────────────────────┐
│                     zigQuant v0.4.0                      │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │   Backtest  │  │  Optimizer  │  │ Live Trading│     │
│  │   Engine    │  │   Engine    │  │   Engine    │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│        │                 │                 │             │
│        │                 │                 │             │
│  ┌─────▼─────┐  ┌────────▼────────┐  ┌────▼─────┐     │
│  │  Single   │  │  Thread Pool    │  │  libxev  │     │
│  │  Thread   │  │  (std.Thread)   │  │ Event Loop│     │
│  └───────────┘  └─────────────────┘  └──────────┘     │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

### 模块分工

#### 1️⃣ 回测引擎（保持单线程）
```zig
// src/backtest/engine.zig
// ✅ 不改动 - 单线程性能已足够
pub const BacktestEngine = struct {
    pub fn run(...) !BacktestResult {
        for (candles.candles) |candle| {
            // 顺序处理，逻辑清晰
        }
    }
};
```

#### 2️⃣ 参数优化器（使用 std.Thread）
```zig
// src/backtest/optimizer.zig
pub const ThreadPool = struct {
    threads: []std.Thread,
    task_queue: std.atomic.Queue(Task),
    
    pub fn runParallel(tasks: []BacktestTask) ![]Result {
        // 并行执行所有参数组合
        // 使用标准线程池，简单高效
    }
};
```

#### 3️⃣ 实时交易引擎（使用 libxev）
```zig
// src/trading/live_engine.zig
const xev = @import("xev");

pub const LiveTradingEngine = struct {
    loop: xev.Loop,
    ws_watcher: xev.TCP,
    strategy: IStrategy,
    
    pub fn start(self: *LiveTradingEngine) !void {
        // 初始化事件循环
        self.loop = try xev.Loop.init();
        defer self.loop.deinit();
        
        // 连接 WebSocket
        var ws = try self.connectWebSocket();
        
        // 注册回调
        var c: xev.Completion = undefined;
        ws.read(&self.loop, &c, .{ .slice = &self.buffer }, 
            LiveTradingEngine, self, onMarketData);
        
        // 运行事件循环
        try self.loop.run(.until_done);
    }
    
    fn onMarketData(
        self_: ?*LiveTradingEngine,
        l: *xev.Loop,
        c: *xev.Completion,
        r: xev.TCP.ReadError!usize,
    ) xev.CallbackAction {
        const self = self_.?;
        const n = r catch |err| {
            // 处理错误
            return .disarm;
        };
        
        // 解析市场数据
        const market_data = self.parseMarketData(self.buffer[0..n]);
        
        // 调用策略（非阻塞）
        self.strategy.onMarketData(market_data) catch {
            // 处理策略错误
        };
        
        // 继续读取
        self.ws_watcher.read(l, c, .{ .slice = &self.buffer },
            LiveTradingEngine, self, onMarketData);
        
        return .rearm;
    }
};
```

---

## 🚀 实施计划

### Phase 1: 依赖添加（v0.3.1）

#### 步骤 1: 添加 libxev 依赖

**build.zig.zon**:
```zig
.{
    .name = "zigQuant",
    .version = "0.3.1",
    .dependencies = .{
        // ... 现有依赖
        
        .libxev = .{
            .url = "https://github.com/mitchellh/libxev/archive/refs/tags/v0.24.0.tar.gz",
            .hash = "1220...", // zig fetch 获取
        },
    },
}
```

**build.zig**:
```zig
const libxev_dep = b.dependency("libxev", .{
    .target = target,
    .optimize = optimize,
});

exe.root_module.addImport("xev", libxev_dep.module("xev"));
```

### Phase 2: 实时交易引擎重构（v0.4.0）

#### 新增文件结构
```
src/trading/
├── live_engine.zig          # 🆕 LiveTradingEngine (libxev)
├── ws_handler.zig            # 🆕 WebSocket 事件处理
├── order_executor.zig        # 修改：异步订单提交
└── position_tracker.zig      # 修改：实时仓位更新
```

#### 核心实现

**1. WebSocket 连接管理**
```zig
// src/trading/ws_handler.zig
const xev = @import("xev");

pub const WebSocketHandler = struct {
    loop: *xev.Loop,
    socket: xev.TCP,
    url: []const u8,
    
    pub fn connect(self: *WebSocketHandler) !void {
        // 使用 libxev TCP 建立连接
        var c: xev.Completion = undefined;
        
        self.socket = try xev.TCP.init(
            self.loop, 
            std.net.Address.parseIp(self.url, 443) catch unreachable
        );
        
        self.socket.connect(
            self.loop, 
            &c, 
            WebSocketHandler, 
            self, 
            onConnected
        );
    }
    
    fn onConnected(
        self_: ?*WebSocketHandler,
        l: *xev.Loop,
        c: *xev.Completion,
        r: xev.TCP.ConnectError!void,
    ) xev.CallbackAction {
        const self = self_.?;
        _ = r catch |err| {
            // 连接失败，重试
            return .disarm;
        };
        
        // 发送 WebSocket 握手
        self.sendHandshake();
        
        // 开始接收数据
        self.startReceive();
        
        return .disarm;
    }
};
```

**2. 策略事件处理**
```zig
// src/trading/live_engine.zig
pub const LiveTradingEngine = struct {
    pub fn onMarketData(
        self: *LiveTradingEngine,
        data: MarketData,
    ) !void {
        // 1. 更新内部状态（非阻塞）
        try self.updateMarketState(data);
        
        // 2. 调用策略生成信号
        const signal = try self.strategy.generateSignal(data);
        
        // 3. 如果有信号，异步提交订单
        if (signal) |sig| {
            try self.submitOrderAsync(sig);
        }
        
        // 4. 更新风控指标
        try self.risk_manager.update(data);
    }
    
    fn submitOrderAsync(self: *LiveTradingEngine, signal: Signal) !void {
        // 使用 libxev 异步提交订单（不阻塞主循环）
        var c: xev.Completion = undefined;
        
        const order_data = try self.buildOrderRequest(signal);
        
        self.order_socket.write(
            &self.loop,
            &c,
            .{ .slice = order_data },
            LiveTradingEngine,
            self,
            onOrderSubmitted,
        );
    }
};
```

### Phase 3: 性能测试（v0.4.0）

#### 测试指标

| 指标 | 目标 | 测试方法 |
|------|------|---------|
| WebSocket 延迟 | < 5ms | 接收到处理完成的时间 |
| 订单提交延迟 | < 10ms | 信号到订单发出的时间 |
| 消息吞吐量 | > 1000/s | 模拟高频行情 |
| CPU 使用率 | < 20% | 空闲时的基线 |
| 内存分配 | 零运行时分配 | libxev 保证 |

---

## 📊 性能提升预期

### WebSocket 性能对比

| 实现方式 | 延迟 | 吞吐量 | CPU 使用 |
|---------|------|--------|---------|
| **同步阻塞**（当前）| ~50ms | ~100 msg/s | 5% |
| **libxev + io_uring** | **~2ms** | **~5000 msg/s** | 8% |
| **提升倍数** | **25x** ⬆️ | **50x** ⬆️ | 1.6x |

### 参数优化性能对比

| 实现方式 | 125 次回测 | 1000 次回测 |
|---------|-----------|------------|
| **单线程**（当前）| 625 秒 | 5000 秒 (83 分钟) |
| **8 核线程池** | **78 秒** | **625 秒** (10 分钟) |
| **提升倍数** | **8x** ⬆️ | **8x** ⬆️ |

---

## 🎯 最终架构图

```
┌────────────────────────────────────────────────────────────────┐
│                        zigQuant v0.4.0                          │
└────────────────────────────────────────────────────────────────┘

┌─────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  Backtest Mode  │  │  Optimizer Mode  │  │  Live Trade Mode │
│  (单线程循环)    │  │  (线程池并行)    │  │  (libxev 事件)   │
└────────┬────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                    │                      │
         │                    │                      │
    ┌────▼────┐        ┌──────▼──────┐       ┌──────▼──────┐
    │  For    │        │ Thread Pool │       │ libxev Loop │
    │  Loop   │        │ (std.Thread)│       │ (io_uring)  │
    └─────────┘        └─────────────┘       └─────────────┘
         │                    │                      │
         ▼                    ▼                      ▼
    ┌─────────┐        ┌─────────────┐       ┌─────────────┐
    │  CSV    │        │  Backtest×N │       │  WebSocket  │
    │  Data   │        │  Parallel   │       │  Async I/O  │
    └─────────┘        └─────────────┘       └─────────────┘

性能特点:
• 回测: 简单可靠，性能够用
• 优化: 并行加速 8 倍
• 实盘: 超低延迟（io_uring）
```

---

## 💡 关键决策

### ✅ 应该做的

1. **v0.3.0**: 按原计划发布（不加 libxev）
   - 理由: MVP 优先
   
2. **v0.3.1**: 添加线程池优化器
   - 理由: 参数优化必须并行
   - 实现: std.Thread（简单）
   
3. **v0.4.0**: 集成 libxev 实时交易
   - 理由: 生产级性能
   - 实现: 参考上述方案

### ❌ 不应该做的

1. ❌ 回测引擎不用 libxev
   - 理由: 单线程已够用，增加复杂度无意义
   
2. ❌ 参数优化器不用 libxev
   - 理由: CPU 密集任务，线程池更合适
   
3. ❌ 不要过早优化
   - 理由: 先验证需求，再优化

---

## 📚 学习资源

1. **libxev 官方示例**:
   - https://github.com/mitchellh/libxev/tree/main/examples

2. **io_uring 原理**:
   - https://unixism.net/loti/what_is_io_uring.html

3. **Proactor vs Reactor**:
   - https://en.wikipedia.org/wiki/Proactor_pattern

4. **Zig 多线程指南**:
   - https://zig.guide/standard-library/threads

---

## 🎉 总结

### libxev 评分

| 维度 | 评分 | 说明 |
|------|------|------|
| **性能** | ⭐⭐⭐⭐⭐ | io_uring，零分配 |
| **易用性** | ⭐⭐⭐⭐ | API 简洁，文档清晰 |
| **稳定性** | ⭐⭐⭐⭐⭐ | Ghostty 生产使用 |
| **生态** | ⭐⭐⭐ | Zig 原生，但生态较新 |
| **跨平台** | ⭐⭐⭐⭐ | Linux/macOS/WASI |
| **维护性** | ⭐⭐⭐⭐⭐ | Mitchell 维护 |

**综合评分**: **4.7/5.0** ⭐⭐⭐⭐⭐

### 最终建议

**强烈推荐在 v0.4.0 集成 libxev！**

**理由**:
1. ✅ 作者权威（Mitchell Hashimoto）
2. ✅ 生产验证（Ghostty）
3. ✅ 性能卓越（io_uring）
4. ✅ Zig 原生（完美集成）
5. ✅ 适用场景（实时交易）

**实施路径**:
- v0.3.0: MVP（4 天）
- v0.3.1: 线程池（3 天）
- v0.4.0: libxev（7 天）← **在此集成**

---

**下一步**: 先完成 v0.3.0 MVP，积累使用经验后再引入 libxev。
